import ActionButtonIcon from 'common/components/buttons/ActionButtonIcon'
import { render } from '@testing-library/react'

describe('ActionButtonIcon', () => {
  it('should display correctly', () => {
    render(<ActionButtonIcon icon="add" />)
    // TODO code tests
  })
})
