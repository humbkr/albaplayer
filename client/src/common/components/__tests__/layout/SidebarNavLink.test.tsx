import SidebarNavLink from 'common/components/layout/SidebarNavLink'
import { render } from '@testing-library/react'

describe('Sidebar', () => {
  it('displays correctly', () => {
    render(<SidebarNavLink to="/test" />)
    // TODO code tests
  })
})
