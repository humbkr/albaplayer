import Modal from 'common/components/layout/Modal'
import { render } from '@testing-library/react'

describe('Modal', () => {
  it('displays correctly', () => {
    render(<Modal id="id" isOpen />)
    // TODO code tests
  })
})
